# Next.js Public Docs Site — Environment Variables

# BUILD-TIME PUBLIC VARS (baked into the JavaScript bundle)
NEXT_PUBLIC_API_URL="http://localhost:3333"

# RUNTIME VARS (available on server-side only)
DOCS_REVALIDATE_SECRET="your-dev-revalidation-secret"
